# Copy this file to .env in the project root before running docker-compose up.

# "mock" (default) uses a deterministic local stand-in for the LLM — no key needed.
# Set to "anthropic" or "openai" to use a real model instead.
AI_PROVIDER=mock

# Only required if AI_PROVIDER is "anthropic" or "openai".
AI_API_KEY=
